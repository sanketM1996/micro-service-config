server:
  port: 8072

eureka:
  instance:
    preferredIpAddress: true

  client:
    fetchRegistry: false
    registerWithEureka: false
    serviceUrl:
      defaultZone: http://localhost:8070/eureka